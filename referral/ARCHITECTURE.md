# Nearby Architecture Overview

## Core Principles
1. **Feature Separation**: Business logic is encapsulated in modular component and service structures.
2. **Design System Adherence**: All components consume token values from `NEARBY_DESIGN_SYSTEM.md`.
3. **Type Safety**: Strictly typed models in `/src/types.ts`.
4. **State Management**: React state with clean event handler callbacks.
